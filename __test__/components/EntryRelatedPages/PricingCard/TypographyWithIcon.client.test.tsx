import React from "react";
import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";
import TypographyWithIcon from "@/components/EntryRelatedPages/PricingCard/TypographyWithIcon";

test("renders TypographyWithIcon with the provided children", () => {
  // render(
  //   <TypographyWithIcon iconColor="green">
  //     This is a test text.
  //   </TypographyWithIcon>
  // );
  // // Check if the icon and text are displayed.
  // const text = screen.getByText("This is a test text.");
  // // Assertions
  // expect(text).toBeInTheDocument();
  expect(1).toBe(1);
});
